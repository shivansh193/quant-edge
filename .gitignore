# Build artifacts
/target/

# Environment & secrets
.env
!.env.example

# Cache & databases
/cache/
*.db
*.sqlite
*.sqlite3

# Logs
/logs/

# Editor & OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Profiling
*.prof
flamegraph.svg
perf.data

# Frontend build output & dependencies
node_modules/
.next/
out/
*.tsbuildinfo

# SQLite sidecar files (the .db itself is already ignored)
*.db-shm
*.db-wal

# Claude Code worktrees (local scratch)
.claude/worktrees/

# NOTE: forward_log/ is deliberately NOT ignored - commit it, so git
# timestamps each day's picks externally.

# Point-in-time replay output (a backtest; must not be mixed with forward_log/)
backfill_log/

# Local cache.db backups (scripts/backup_cache.*)
backups/
