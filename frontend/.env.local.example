# Copy this to .env.local for local development.
# In dev, Next.js runs on :3000 and the Rust backend on :8080.
# NEXT_PUBLIC_API_BASE_URL tells the browser where to find the API.
# Leave empty for production (static export served by the Rust server on the same origin).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
